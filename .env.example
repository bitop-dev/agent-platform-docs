# Agent Platform — Docker Compose Environment
# Copy to .env and fill in values

# Required
JWT_SECRET=your-jwt-secret-here
ENCRYPTION_KEY=                    # 32-byte hex key (openssl rand -hex 32)

# Optional
DEFAULT_MODEL=gpt-4o
DEFAULT_PROVIDER=openai
